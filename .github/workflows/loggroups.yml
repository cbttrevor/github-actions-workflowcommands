name: Log Groups

on:
  push:
  workflow_dispatch:

jobs:
  loggroups:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo '::group::'
        echo 'Here is a log message'
        echo '::endgroup::'
    
